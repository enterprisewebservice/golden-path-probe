apiVersion: v1
kind: Service
metadata:
  name: golden-path-probe
  namespace: agent-office
  labels:
    app.kubernetes.io/name: golden-path-probe
    app.kubernetes.io/part-of: supply-chain-services
spec:
  selector:
    app.kubernetes.io/name: golden-path-probe
  ports:
    - name: http
      port: 8080
      targetPort: 8080
